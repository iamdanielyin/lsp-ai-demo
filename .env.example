# 复制为 .env；使用 python scripts/init_local.py 可生成本机随机启动凭证。
PORT=8000
DATABASE_PATH=data/lsp.sqlite3
FILE_DIRECTORY=data/files
CONFIG_MASTER_KEY=<FERNET_KEY>
ADMIN_INITIAL_PASSWORD=<LONG_RANDOM_PASSWORD>
